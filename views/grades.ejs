<!doctype html>
<html lang="en">
<head>
	<% include header %>
</head>
<body>
	<% include navbar %>
	GRADES!!
	<% if (user.owner) { %>
		<p>OWNER</p>
	<% } %>
	<% if (user.student) { %>
		<p>STUDENT</p>
	<% } %>
	<% if (user.tutor) { %>
		<p>TUTOR</p>
	<% } %>
</body>
<% include footer %>
</html>